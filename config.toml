baseURL = "http://metzgr.github.io/beta-performance-gov/"
defaultContentLanguage = "en"
title = "Performance.gov"
theme = "pgov"

[outputFormats]
  # Ensure there are no custom settings here.

[outputs]
  home = ["HTML"]  # Add this line to specify the homepage layout
